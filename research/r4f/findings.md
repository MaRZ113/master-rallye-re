# R4F automated findings

R4E.1 was closed first in commit `99ba5a3`; N1 normal writing and M1 environment feature writing are confirmed by human runtime observation. The **SAME-TOPOLOGY VEHICLE SDK V1 BASELINE** is frozen.

The 78-file vehicle corpus contains 1,478 physical draws. Vertex and index ranges are contiguous, monotonic and disjoint in all 78; all 1,478 draws reference every declared vertex. The existing local/global triangle relationship is exact. All files have a marker-1339 44-byte bounds footer after collision; footer min/max equals the render/collision union and its center is the midpoint. One scalar remains unknown and is preserved. No parsed header, draw, global, collision or footer field is an absolute file offset into the render core. The writer rejects unrecognized suffixes and keeps edits within original bounds.

The experimental rebuilder copies the prefix, material records except four proven range fields, and the entire collision/footer suffix. It compiles existing-draw vertices, local uint16 triangles and global uint32 indices deterministically. Blender source IDs are retained where unique; UV, normal, color and draw-boundary variations split vertices rather than averaging. New faces need an existing draw assignment or unique material inference. Same-topology exports still use the established R3/R4E byte-patch route.

F1 uses Astero `car.dx` body draw 7 (`acamo64b-tga` / `whitepaint-tga`), source triangle 27 with global corners 1227/1228/1229. Its geometric outward normal is approximately `(0.08964, 0.97022, 0.22502)`; three duplicated corners move 0.05 source units along that vector, staying strictly inside the original render-only bounds. Counts change 2543→2546 vertices and 2021→2022 triangles; the render-core end moves 130999→131125. The ignored candidate SHA-256 is `b03e8402baef682bb5665ccbce3a9cc8342e87409b3a5bd2170e9674d6ed7835`; unexplained external diffs: 0.

The first full dry run found existing source anomalies: declared top-level count warnings in some resources and a pre-existing degenerate triangle in Pajero/complete.dx. Preserving these produced **78/78 parsed, rebuilt, reparsed, semantic-equivalent and byte-identical** zero-edit outputs; no blocks or failures. The F1 Astero hood-side draw-7 candidate adds 3 vertices and 1 triangle, changes only the render core and four required range fields, and leaves collision/suffix bytes identical. The Blender edit-mode workflow exported the exact same SHA-256 as the direct writer.

**Automated verdict: READY FOR HUMAN TOPOLOGY TEST. Runtime validation: WAITING FOR HUMAN.** No claim is made about topology behavior in the original game until F1 is tested. Out-of-bounds geometry, new draw/material creation, complete removal of a draw, non-triangulated faces and arbitrary collision edits remain unsupported.
