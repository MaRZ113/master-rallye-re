# demo-8.4.1 manifest

- Files: 3898
- Bytes: 298491020
- Unique extensions: 20
- Extension counts: `.gxi` 2046, `.dxt` 1181, `.xml` 184, `.xml#` 107, `.gxm` 76, `.dxb` 64, `.txt` 51, `.dx` 49, `.gxb` 40, `.wav` 38, `.mp3` 21, `.map` 17, `.fl` 8, `.hnt` 5, `.tga` 3, `.jpg` 2, `.sf` 2, `.exe` 2, `.psd` 1, `.ico` 1
- Image structure scan: .gxb:valid 40, .gxi:error 1, .gxi:valid 2045

Every manifest entry retains corpus_id and relative_path. No game file is copied into Git.
