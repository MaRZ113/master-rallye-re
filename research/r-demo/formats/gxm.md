# GXM continuation evidence

Canonical prefix and geometry structure are in `docs/formats/gxm.md`. A human Trooper 8.4.1 body-position edit confirms Vector C live position geometry (**CONFIRMED_BY_RUNTIME**). Removing `car.gxm`, `complete.gxm` and `wheel.gxm` isolates their tested race-body, presentation and visual-wheel roles. The `$chull` source object is disjoint from other Trooper Vector C references and has an ignored rigid-translation candidate, but its collision-specific runtime effect remains **UNKNOWN**. See `../runtime/loader-matrix.md`, `gxm-live-loading.md` and `chull-test.md`.
