# R-DEMO runtime validation plan

Status: first-pass human Trooper 8.4.1 GXM edit and car/complete/wheel removal tests are **CONFIRMED_BY_RUNTIME** as reported by the user. Their detailed matrix is in `runtime/loader-matrix.md`. Clean DXT regeneration, isolated `$chull`, live Debug capture and file-access trace remain pending; use the more specific protocols in `runtime/`.

## Scratch setup

Create an isolated runnable copy of `demo-8.4.1` outside the authoritative corpus. Keep `D:\Game\Master Rallye\corpora\demo-8.4.1` untouched. Use a distinct directory such as `.research-output/r-demo/scratch/run-8_4_1/`; this is Git-ignored. Copy only the dependencies needed for a runnable Trooper test. Record a pre-test file listing and SHA256 of each file to be changed. Never delete or modify a source-corpus file.

## DXT regeneration

Use `DataGx/Vehicles/Trooper/Black-tga.gxi` and the same-build `black-tga.dxt`. The authoritative DXT SHA256 in the manifest is `c8af53e3c1ea06c42178b3e1988dff0b3c64f150b722cba6bdd0450dc1357f82`; their complete static GXI→DXT mapping is byte-identical. In the runnable scratch copy only, remove `black-tga.dxt`, launch demo 8.4.1, enter the Trooper context that requests this texture, exit, and hash any regenerated DXT. Compare complete bytes, header words, CRC, and payload with the manifest version. If no file appears, record the path and event; do not infer that regeneration is impossible. Process Monitor capture should filter by demo PID and the two exact filenames.

## GXM controlled edit

The candidate is `.research-output/r-demo/scratch/demo-8.4.1/DataGx/Vehicles/Trooper/car.gxm`, generated by `tools/scanner/r_demo_gxm_candidate.py`. It has SHA256 `2e9aa9b2c8bbcf81b5ae3269bc0d8475d38f52d8c537e935fc1b3771399e3b3d`; source SHA256 is `fd08bc10c440fce261ef126e40445002fb56359d4011c1dcbf9c2f71b0e91047`. Vector C index 646, component 2, changes from approximately 1.48493 to 1.63493. Eight indexed records reference it. Three bytes at offsets 213541–213543 differ, all within that one float32. Size, material table, index counts, and parsed arrays remain valid.

The user subsequently reported that a structurally identified Vector C edit visibly changed the Trooper body in demo 8.4.1. This is **CONFIRMED_BY_RUNTIME** for live position geometry, although the exact tested binary hash was not provided with the new report. The old one-float candidate remains an ignored structural artifact; see `runtime/gxm-live-loading.md`. The next distinct test is the isolated `$chull` candidate in `runtime/chull-test.md`.

## Loader matrix and trace

Human first-pass GXM+DX, GXM-only and DX-only visual results are now recorded separately for `car`, `complete` and `wheel` in `runtime/loader-matrix.md`. A minimal repeat with Debug capture and ProcMon is still needed to determine actual file opens/reads/writes and DX role. Use clean scratch baselines, filter by demo PID and exact resource path, and record build version, hashes, visible result and event sequence. The neither-file case was not reported. See `runtime/file-access-correlation.md`; no EXE patching is part of this plan.
