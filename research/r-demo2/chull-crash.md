# Isolated `$chull` candidate: crash localized to convex-hull build

The user-provided 8.4.1 DebugView crash capture names Trooper `car.gxm`. Runtime messages show `Inserting moSortPlane nodes -` → `done`, then `Vertex welder -` → `done`, then `Building convex hull -`. The log ends there. No completion line follows and no later BSP, cylinder, 2D geometry, land database, object node, optimization, or cache serialization stage appears.

Classification: **CRASHED_IN_RUNTIME**. Last completed stage: vertex welder. Last started stage: convex-hull build. Crash localization: inside the convex-hull build stage (**CONFIRMED_BY_RUNTIME_TRACE**). The normal control trace runs the same stage to `done`, so the trace parser distinguishes the crash from an intentionally skipped stage.

The tested position-only `$chull` candidate hash for the 8.4.1 scratch asset was `0425e1636bc83844da241d48e30fedae2d48aff024cb4f1e329506ac64b1e699`. The same-build 9.3.1 source-to-generated tag101 correspondence is documented in `gxm-to-tag101.md`, but does not identify the invariant that the edit violated. Vector A/B/C, indexed records, winding, plane offsets, bounds, hierarchy, and other dependent data remain candidates for static dependency analysis; no cause is promoted without evidence.

Do not produce a second blind `$chull` mutation. The 9.3.1 DebugView test yielded **NO_OUTPUT_OBSERVED**, not proof of logger removal.

## R-DEMO2.2 static dependency result

A fresh Ghidra analysis of demo 9.3.1 (EXE SHA256 `931cfc4e0c520c26581b0c1173d1beb586facd17176b885666f455090f646680`) identifies `FUN_005de630` as the `$chull` orchestrator and `FUN_005df370` as its recursive child-mesh triangle-position collector. The orchestrator gathers child counts, allocates input and calls subsequent hull processing. This establishes that the cooker input is a recursive node/tree geometry stream, not merely the sidecar's flat record interval. The collector's internal `+0x1c/+0x20/+0x24` fields are post-loader record offsets; their correspondence to raw serialized GXM words has not been demonstrated.

The selected Trooper/Jump/NewRav/Tata hull records all store Vector-B sentinel indices. Vector A remains near the source face normal after the axis transform (Trooper max angle 7.934 degrees), while the C-only rigid translation preserves that geometry. These checks narrow the likely dependency set but do not reveal the exact failed substage or prove which descendants/aliases the candidate omitted. Structural tree/record mismatch is a hypothesis, not a confirmed crash cause. No second runtime mutation was attempted. Full measurements and the one-ULP-sensitive marker checks are in `collision-cooker.md`.

## Offline invariant check (not a runtime attempt)

A scratch-only copy of 9.3.1 Trooper GXM was generated by translating all 36 Vector-C ids referenced in the 68-record `$chull(Trooper)` interval by +0.4 source X. The flat GXM checker passes: record bytes/topology are unchanged, no selected C ids are shared outside the interval, all changes are limited to those C values, A/B are unchanged, the indexed faces remain nondegenerate, and rigid-delta residual is `3.27e-8`. Source-face normal/area changes are at float32 noise scale. Candidate SHA256 is `32fcbc212f226a307a3d71c43c3cbedabcf32526f86961a3bd1af16ad3c1aac4`.

This is not the user's crashed runtime asset and was not launched. It demonstrates that the flat record interval's obvious topology/rigidity invariants do not explain the observed crash. `FUN_005df370` recursively consumes the selected internal node and descendants; serialized-GXM-to-loaded-tree coverage remains the key missing dependency. No second runtime candidate was attempted.


## R-DEMO2.4 loader-to-runtime-node reconstruction

### Decision gate

**DECISION GATE: UNRESOLVED (Path U).** Neither an incomplete flat span (Path A) nor exact runtime-input equivalence (Path B) is proven. No second runtime `$chull` mutation was attempted.

### Loader and serialized nodes

The exact read-only demo 9.3.1 Trooper `car.gxm` is 222,754 bytes, SHA256 `5caed6da1213f17e2638d0ee47860cf521d4715475418e028bfd13f7c0e87642`. Its geometry-prefix parser places the 732-byte tail at `0x36346`. The tail starts with length-prefixed `Model`, then parses exactly to EOF as 28 recursive nodes (two top-level records). `shell` has 19 direct children; nested `interior`/crew and `spare`/hub records match the 9.3.1 TXT-sidecar braces and mesh spans.

For this corpus, every node uses serialized type 1/version 1. The record layout is `[u8 type, u8 version, u16 child_count, u32 mesh_start, u32 mesh_count, u16 name_length, name, child records...]`. The order and field association are confirmed by the exact 9.3.1 executable's `FUN_005bea30`: it reads the four-byte header, dispatches type/version, reads the eight-byte mesh range for type 1, reads the name, recursively reads `child_count` records, then attaches them. `FUN_005cd250` allocates the 0x24-byte mesh node and initializes the mesh range at object offsets `+0x1c/+0x20`. The root name is outside this per-node header; two top-level nodes follow through EOF. That top-level EOF termination is **HIGH_CONFIDENCE_INFERENCE** from the exact bytes and recursive parser; the outer caller `FUN_005c98d0` still needs a focused decompilation to confirm its loop.

The matching sidecar and the parsed raw hierarchy agree: `Model` → `shell` → its 19 children, plus root-level `$chull(Trooper)`. The hull is a leaf, type 1/version 1, range `[1911,1979)`, record count 68. The full parsed tree and byte offsets are in `hull-hierarchy.json`; parser source is `src/master_rallye/gxm_hierarchy.py` and the repeatable report tool is `tools/scanner/r_demo2_gxm_hierarchy.py`.

### Runtime object layout from exact EXE

| Offset | Meaning | Evidence |
|---:|---|---|
| `+0x00` | vtable/type dispatch | **CONFIRMED_BY_EXE**, virtual calls through it |
| `+0x04` | first child | **CONFIRMED_BY_EXE**, node attachment and traversal |
| `+0x08` | next sibling | **CONFIRMED_BY_EXE**, node attachment and traversal |
| `+0x0c` | parent | **CONFIRMED_BY_EXE**, attach/detach helper |
| `+0x10` | last child | **CONFIRMED_BY_EXE**, append bookkeeping |
| `+0x14` | name pointer | **CONFIRMED_BY_EXE**, name assignment and `$chull` search |
| `+0x1c` | mesh record start | **CONFIRMED_BY_EXE**, mesh accessor and hull collector |
| `+0x20` | mesh triangle count | **CONFIRMED_BY_EXE**, mesh accessor and hull collector |

`FUN_004b1a60` maintains child head, sibling link, parent pointer and last-child pointer. `FUN_005c9710` initializes base-node link/name fields; the type-1 constructor adds the mesh range. The GXM parser's nesting and order therefore map to the EXE's child/sibling representation for the 28 type-1 Trooper records. Native process addresses and debugger-observed objects were not captured; this is a static reconstruction, **CONFIRMED_BY_EXE**, not **CONFIRMED_BY_RUNTIME**.

### `$chull` selection and traversal

`FUN_005de630` selects a runtime name containing `$chull`; `FUN_005dea80` searches the root and descendants with the observed fixed-depth nested walk. For Trooper's serialized tree there is one matching node, `$chull(Trooper)`, at the root-level sibling position after `shell`; it has no child. The selected node therefore has no hierarchy descendants in the serialized mapping. Whether the name helper performs substring/prefix matching or exact directive parsing beyond the tested name remains bounded by the caller's extension handling.

`FUN_005df060` counts each current node's mesh triangle count times three and recursively accumulates child meshes in stored sibling order. `FUN_005df370` then emits current-node mesh triangles first, and recurses children in sibling order. For each triangle it emits three indexed Vector3 positions in internal record corner order. No node-local transform is applied in this collector. Its position buffer comes from the loaded object's mesh-data pointer chain (`+0x38`, then buffer `+4`). The exact EXE collector order is **CONFIRMED_BY_EXE**; its input is the post-load internal mesh store, not raw GXM records.

### Serialized hull span versus actual collector input

Offline parsing reproduces the serialized/pre-normalizer hull stream: 68 ordered record IDs `[1911,1979)` with 36 unique Vector-C indices. It is not yet the exact stream consumed by `FUN_005df370`.

Before that collector, `FUN_005c9990` recursively visits nodes. For eligible positive even-sized meshes it processes source triangles in pairs through `FUN_005ca370`. On success, it copies a generated 0x34-byte record buffer into a shared store and rewrites the node's `+0x1c/+0x20` range to the compacted store. The branch may leave mesh ranges untouched when the normalizer rejects or skips the candidate. We have not established which branch `$chull(Trooper)` takes, nor mapped generated internal record fields back to the serialized C triples. Therefore the 68 raw source IDs and 36 C indices do not prove the final runtime emitted positions, their coordinates, or even an unchanged record count.

No transform appears in `FUN_005df370` itself. `FUN_005ca370` does construct/test rotated mesh data in an optimization path; whether a rewritten `$chull` range points to unchanged coordinates, reordered records or a different representation remains **UNKNOWN** for this exact node.

### Path U: one remaining exact mapping

The missing mapping is the `$chull(Trooper)` result of `FUN_005c9990`/`FUN_005ca370`: determine whether it is rewritten and, if so, map the final 0x34-byte internal records' three position indices and backing Vector3 storage back to raw GXM record IDs/Vector C positions. This is the single evidence gate before classifying flat `[1911,1979)` as exact/subset/superset/remapped or preparing any new mutation.

The prior C-only candidate remains validated only against the flat serialized interval. Its runtime result is the existing crash evidence; no new attempt occurred. The 9.3.1 DebugView capture remains `NO_OUTPUT_OBSERVED`, not evidence of a specific runtime crash point. See `hull-runtime-input.md` for the evidence ledger and `collision-cooker.md` for the pre-normalizer triangle indices and earlier invariants.
