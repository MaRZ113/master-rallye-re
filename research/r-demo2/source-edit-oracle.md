# 9.3.1 visible source edit → original cooker oracle

A scratch-only source candidate is prepared at `.research-output/r-demo2/source-edit/DataGx/Vehicles/Trooper/car.gxm`, SHA256 `4f6dce6a90c1824955fbd9bfa9259166e96bca07bb046a95635aae994b7c8f2d`. Its unmodified same-build source has SHA256 `5caed6da1213f17e2638d0ee47860cf521d4715475418e028bfd13f7c0e87642`. Vector C index 646 belongs to eight visible-body records (921, 922, 1002, 1003, 1012, 1014, 1015, 1016), outside `$chull` records 1911–1978. Only component 2 changed, `1.4849326610565186 → 1.6349326372146606`, affecting three bytes at offsets 213546–213548. This is a **STRUCTURAL_CANDIDATE_ONLY** in 9.3.1; no runtime result is claimed.

In a separate runnable 9.3.1 copy, first delete only scratch `car.dx` with unchanged `car.gxm`; load Trooper and preserve the generated DX as baseline. Restore the same scratch state, replace only scratch `car.gxm` with this candidate, delete scratch `car.dx`, load Trooper and preserve the generated modified DX. Hash both, record whether both loads succeed and the visible change, then run `tools/scanner/r_demo2_compare_dx.py` on the two generated files. Do not substitute the historically shipped DX for the regenerated baseline. No `$chull` edit is part of this experiment.

## Supplied original-cooker output: byte differential

A new `gxm_custom_rebuild/car.dx` appeared in the ignored input directory. It is 124,568 bytes, SHA256 `9d3e8d4181ddd4929d777709d37dc4a1177a4b4a5ae4f2a3bf4b9042a6ebe655`. Its source association is supported by the folder label and by the exact `+0.1499999761581421` mapped position delta; the exact scratch GXM hash was not supplied, but the user has now reported the custom rebuild loads and the edit is visible. The comparison baseline is rebuild-A/B SHA256 `8238078c40f7b419b2f3cc3a14f4511f1cdb6a8bce00f3589a39fbfcde32d5b1`, not the historical shipped DX. Full metadata is ignored at `.research-output/r-demo2/baseline-vs-gxm-custom.json`.

**CONFIRMED_BY_BYTES:** exactly one DX render position changed: vertex 1224, Y `1.4849326610565186 → 1.6349326372146606`. This matches the source Vector C index 646 component-2 edit under `(x,y,z)→(x,z,-y)`. All 2,310 normals, colors, UV, 5,733 local/global indices, 1,638 raw draw/material bytes and the entire 5,296-byte tag101 blob are byte-identical. No topology or collision byte changed. Marker-1339 changed: Y center `0.7112241983413696 → 0.7503586411476135`, radius `2.5821099281311035 → 2.5931355953216553`, and Y maximum `1.5566637516021729 → 1.6349326372146606`; minimum and other axes are unchanged.

The entire DX diff is only 11 changed bytes, in four ranges: `[14708,14711)` (position Y) and `[124532,124535)`, `[124540,124542)`, `[124560,124563)` (marker-1339 float fields). No other byte differs. The generic comparator correctly returns `UNRESOLVED` rather than calling a deliberate 0.15-unit visible edit mere float drift; the bounded differential itself is clear.

This proves a specific original-cooker source-position→compiled-position mapping and observed bounds regeneration in the supplied artifacts. The user reports that this custom 9.3.1 DX loads even with GXM absent and that the geometry edit is visible (**CONFIRMED_BY_RUNTIME**, human). This closes the tested source-edit→compiled-DX→DX-only visual path; exact scratch GXM provenance remains a recorded limitation. The non-hull source edit did not alter tag101 or normals in this run.
