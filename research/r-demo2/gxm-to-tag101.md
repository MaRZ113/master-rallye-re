# Same-build 9.3.1 Trooper `$chull` to regenerated tag101

Source: `demo-9.3.1:DataGx/Vehicles/Trooper/car.gxm` (same-build sidecar `Trooper.txt`); target: the supplied 9.3.1 runtime-regenerated `car.dx`, SHA256 `8238078c40f7b419b2f3cc3a14f4511f1cdb6a8bce00f3589a39fbfcde32d5b1`. Source assets were read only; target binary remains ignored.

The sidecar identifies `$chull(Trooper)` records 1911–1978 (68 records). Their indexed triples reference 36 distinct Vector C positions. Under `(x,y,z) → (x,z,-y)`, a convex-hull vertex enumeration finds 28 of those points. Regenerated tag101 Representation B geometry A also has 28 vertices and 52 triangles. Nearest-neighbor correspondence is bijective, with maximum distance `6.677050543111643e-6` in either direction. The 28 mapped B vertices are distinct source points; this is **CONFIRMED_BY_BYTES / same-build cooker artifact** numerical evidence.

Tag101 Representation A has an eight-corner box whose axis extrema differ from the transformed source-hull extrema by at most about `1.19e-7`. Its center-to-box-corner distance differs from the stored tag101 base scalar `2.6045312881469727` by about `4.3e-8`. The 68 source triangle triples have 68 unique unordered triples; B has 52, of which only 16 match unordered source triples after nearest-point mapping. The cooker therefore constructs a different convex surface triangulation rather than simply copying the source triangle list. Exact source winding/face/plane rules remain unresolved. Marker-1339 includes render geometry and must not be attributed solely to `$chull`.

This same-build regenerated mapping removes the old retail cross-build caveat and strengthens the `$chull`→tag101 source hypothesis. It does not explain why the source-only hull translation candidate crashed, nor does it prove exact runtime collision behavior for that candidate. `$cylinder`→tag102 mapping remains **UNKNOWN** for this resource because the parsed target contains tag101 but no tag102.

The numeric mapping can be reproduced with `tools/scanner/r_demo2_chull_map.py` using 9.3.1 `car.gxm`, its sibling `Trooper.txt`, and ignored `rebuild-A/car.dx`; the metadata output is `.research-output/r-demo2/same-build-chull-map.json`.
