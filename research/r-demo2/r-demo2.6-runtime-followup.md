# R-DEMO2.6 — one bounded x32dbg follow-up (exact demo-8.4.1)

Purpose: distinguish the first no-match face/endpoint input in the original and the pinned `0425e163...` candidate. This is a **read-only debugger observation** in separate disposable 8.4.1 runtime copies. Do not modify EXE bytes, assets, corpora, or the research worktree. Keep raw captures under ignored `.research-output/r-demo2/runtime/` with file/hash/build identity.

1. Verify the exact `demo-8.4.1/MRallye.exe` SHA256 `bbdfdb709ed41b10461b233b2f5c55403f1b6640f57d9e440476211e51ce75be` and baseline/candidate GXM hashes in the main report. Ensure the module is loaded at preferred `0x400000`; if not, translate addresses by the module-base delta and record it.
2. After the `Building convex hull -` stage starts for Trooper, enable one breakpoint at **`005C447B`** (the no-match branch). For the candidate, stop on the *first* hit before `XOR EDI,EDI` executes. Record registers, stack, `ESI` (current face), `EBX` (current edge), `[ESI+28]` (face-list head), `[ESI+2C]` (remaining count), `[ESP+34]` (local-list head), and the first two `+0/+4/+8` dwords of each linked list node. Dump current edge's `0x80` bytes and both endpoint nodes under `[EBX+64]`, including their `+8` vertex pointers. Save the corresponding `0x20` bytes for each vertex object. Record the face object's `0x40` bytes. Avoid stepping into rendering or altering register values.
3. Run the untouched baseline with the same breakpoint/stage, and note whether it ever hits before hull `done`. If it does, capture the same fields and whether any later NULL call follows. A non-hit alone says this branch was not observed in that run, not that all possible inputs are safe.
4. If the candidate's first `005C447B` does not lead to the reported `005C4457`/`EBX=0`, retain that discrepancy. Otherwise the one capture resolves the static-producer-vs-runtime gap. Only if the first capture does not identify the compared endpoint identities, add breakpoint **`005C4457`** scoped to that face and log `EBX`, `EDI`, and `AL` after the call; stop once the first no-match case is explained. Avoid an always-on conditional breakpoint on this hot loop during the full five-minute load.

What to send back: register/memory text dump or screenshots with addresses, exact SHA256s, whether each baseline/candidate breakpoint fired, and final `Building convex hull` outcome. Do not send entire game assets. This is a capture request, not an authorization for another GXM mutation.
