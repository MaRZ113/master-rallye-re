# Conditional duplicate-Astero human test

**Not ready.** There is no patched copy or manifest. This plan is for use only after `slot25-patch-plan.md` gates are closed and a deterministic, hash-locked patcher produces an ignored local copy in `.research-output/r5v_b/runtime-test/`.

Use a backed-up retail installation and a disposable profile; launch only the patched executable copy, and avoid saving after the test. Check that the game boots and the menu loads. Enter vehicle selection, count class-2 entries, cycle through all entries, and note whether an additional item appears at local index 11 and what name it displays. Select it and start a quick race. Confirm with instrumentation or a trustworthy observation that the absolute ID is 25; a duplicate model alone is not ID proof. Check Astero body, wheels, handling/physics, camera, damage, race finish, exit to menu, and the original Astero at ID16. Record crashes and save/profile changes.

Report: GAME BOOTS; MENU LOADS; CLASS-2 COUNT; EXTRA ENTRY; DISPLAY NAME; SELECTABLE; ID25 VERIFIED; RACE LOADS; BODY; WHEELS; PHYSICS; CAMERA; DAMAGE; FINISH/EXIT; ORIGINAL ID16 WORKS; CRASH; SAVE EFFECTS. No "extra slot confirmed" claim is allowed before this human test.
